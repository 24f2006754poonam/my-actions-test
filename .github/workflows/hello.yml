name: My First Action

on: [push, workflow_dispatch]  # Runs on code push or manual trigger

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: 24f2006754@ds.study.iitm.ac.in - Hello World!
        run: echo "Hello from GitHub Actions! Your email step worked."
